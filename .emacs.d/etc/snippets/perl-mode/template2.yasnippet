# -*- mode: snippet -*-
# name: template2 (package)
# key: template
# --
package ${1:`
  (let ((full-path (expand-file-name buffer-file-name))
        (lib-path (expand-file-name (plcmp--get-lib-path))))
    (replace-regexp-in-string "/" "::" (substring full-path (+ (length lib-path) 1) -3)))`};
use strict;
use warnings;

$0

1;
